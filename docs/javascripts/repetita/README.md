Copied from `src/repetita/web/static/` by `scripts/gen_types_doc.py`, so that
the examples on the exercise-types page are rendered by the app's own code
rather than drawn to look like it.

**Do not edit anything here.** Change the originals and regenerate; CI checks
that this copy matches.
